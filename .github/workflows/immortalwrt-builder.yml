name: Build ImmortalWrt R6220

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'ImmortalWrt tag to build'
        required: true
        default: 'v24.10.4'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TAG: ${{ github.event.inputs.tag }}

    steps:
    # 1️⃣ Checkout repository
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    # 2️⃣ 安装官方构建环境（解决 python3-distutils 和依赖问题）
    - name: Install ImmortalWrt build environment
      run: |
        sudo bash -c 'bash <(curl -s https://build-scripts.immortalwrt.org/init_build_environment.sh)'

    # 3️⃣ Clone ImmortalWrt 源码
    - name: Clone ImmortalWrt
      run: |
        git clone --branch $TAG --depth 1 https://github.com/immortalwrt/immortalwrt.git immortalwrt

    # 4️⃣ 修改 R6220 DTS 扩展 overlay
    - name: Modify DTS for R6220 overlay
      run: |
        cd immortalwrt/target/linux/ramips/dts
        # 扩大 rootfs/overlay，把 reserved 并入
        sed -i '/partition@500000 {/,/};/ s/<0x500000 0x01c00000>/<0x500000 0x05800000>/' mt7621_netgear_r6220.dts
        sed -i '/partition@2100000 {/,/};/d' mt7621_netgear_r6220.dts
        cd ../../../..

    # 5️⃣ 更新 feeds
    - name: Update feeds
      run: |
        cd immortalwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    # 6️⃣ 配置默认 target + 默认软件包
    - name: Set default config for R6220 with packages
      run: |
        cd immortalwrt
        make defconfig
        # 设置 R6220 target
        echo "CONFIG_TARGET_ramips=y" >> .config
        echo "CONFIG_TARGET_ramips_mt7621=y" >> .config
        echo "CONFIG_TARGET_ramips_mt7621_DEVICE_netgear_r6220=y" >> .config
        # 默认软件包：UPnP + 中文包
        echo "CONFIG_PACKAGE_luci-app-upnp=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
        echo "CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y" >> .config
        make defconfig

    # 7️⃣ 编译固件
    - name: Compile firmware
      run: |
        cd immortalwrt
        make -j$(nproc) V=s

    # 8️⃣ 准备 Release 目录
    - name: Prepare artifacts
      run: |
        mkdir -p release
        cp immortalwrt/bin/targets/ramips/mt7621/*-r6220*.bin release/

    # 9️⃣ 创建 GitHub Release
    - name: Create GitHub Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.TAG }}
        name: "ImmortalWrt R6220 ${{ env.TAG }}"
        body: "Automated build of ImmortalWrt R6220 ${{ env.TAG }} with overlay expanded and UPnP/Chinese packages"
      env:
        GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}

    # 🔟 上传固件到 Release
    - name: Upload Release Assets
      uses: softprops/action-gh-release@v1
      with:
        files: release/*.bin
      env:
        GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
